{
  "name": "martyx-industries-monorepo",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "npm --prefix martyx-industries-nextjs run dev",
    "build": "cd martyx-industries-nextjs && rm -rf node_modules package-lock.json && npm install && npm run build",
    "start": "npm --prefix martyx-industries-nextjs run start"
  }
}
